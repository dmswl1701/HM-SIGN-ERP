# CLAUDE.md — HM SIGN ERP 프로젝트 가이드

이 파일은 Claude Code가 이 프로젝트에서 작업할 때 자동으로 읽는 지침 파일이다.
프로젝트 루트에 `CLAUDE.md`라는 이름으로 두면 세션 시작 시 자동으로 컨텍스트에 로드된다.

> 이 프로젝트는 **HM SIGN ERP**다. Claude Code를 단순한 AI가 아니라
> **"AI 직원들이 함께 일하는 회사"** 형태로 운영한다.
> 큰 작업은 반드시 `.claude/agents/`의 전문 에이전트(AI 직원)에게 위임해 병렬로 처리한다.

---

## 0. 프로젝트 개요 (HM SIGN ERP)

- **프로젝트명:** HM SIGN ERP
- **운영 주체:** HM인테리어 (간판 디자인·제작·시공·A/S, 인천 기반 가족 경영)
- **기술 스택:** 단일 `index.html` (HTML + CSS + JavaScript) + Supabase + Kakao API
- **호스팅:** Cloudflare Static Assets
- **성격:** 프로덕션 운영 중인 실사용 ERP (프로젝트 생애주기, 견적서·거래명세서 생성,
  견적 이미지 자동 생성 + 카카오톡 공유, CRM, 캘린더, 동선 계획 등)

### 절대 원칙 (모든 작업·모든 에이전트 공통)

이 원칙은 어떤 요청보다 우선한다. 사용자가 명시적으로 "이건 예외"라고 지시하지 않는 한 반드시 지킨다.

1. ✅ **기존 기능 삭제 금지** — 새 기능은 언제나 *추가만* 한다.
2. ✅ **기존 UI / 색상 / 디자인 / 레이아웃 유지** — 개선은 하되 기존 스타일을 깨지 않는다.
3. ✅ **기존 Supabase 구조(테이블·컬럼·정책) 변경 금지** — 필요한 건 추가로만.
4. ✅ **기존 API(Supabase·Kakao) 연동 방식 유지.**
5. ✅ **Cloudflare Static Assets 호환 유지** — 빌드 스텝·번들러 도입 금지.
6. ✅ **`index.html` 단일 파일 유지** — 파일을 쪼개지 않는다.
7. ✅ **기존 배포 방식 절대 변경 금지** (아래 2번 배포 규칙 참조).

**요약: 기존 것을 깨뜨리지 마라. 새 기능은 항상 추가만 한다.**

---

## 1. 로컬 연결 (Claude Code 설치·실행)

### 설치

```bash
# npm으로 설치 (Node.js 18+ 필요)
npm install -g @anthropic-ai/claude-code

# 또는 데스크톱 앱 사용 (Mac/Windows 지원)
# https://claude.ai/download
```

### 실행

```bash
cd /path/to/HM-SIGN-ERP   # 프로젝트 폴더로 이동
claude                    # 대화형 세션 시작
```

- 처음 실행하면 브라우저로 Anthropic 계정 로그인(OAuth)을 안내한다.
- `claude "질문"` — 일회성 질문 실행
- `claude -c` — 직전 세션 이어서 계속
- VS Code / JetBrains 확장으로 IDE 안에서도 사용 가능

---

## 2. 필수 세팅

### 설정 파일 위치

| 파일 | 용도 |
|------|------|
| `~/.claude/CLAUDE.md` | 전역 지침 (모든 프로젝트 공통) |
| `<프로젝트>/CLAUDE.md` | 프로젝트별 지침 (이 파일) |
| `~/.claude/settings.json` | 전역 설정 (권한, 훅, 환경변수) |
| `<프로젝트>/.claude/settings.json` | 프로젝트 설정 (팀 공유, git 커밋) |
| `<프로젝트>/.claude/settings.local.json` | 개인 설정 (git 제외) |
| `<프로젝트>/.claude/agents/*.md` | **AI 직원(서브에이전트) 정의** |

### 권한(permissions) 예시 — `.claude/settings.json`

```json
{
  "permissions": {
    "allow": [
      "Bash(git status)",
      "Bash(git diff:*)",
      "Bash(git add:*)",
      "Bash(git commit:*)",
      "Bash(git push origin main)"
    ],
    "deny": [
      "Bash(rm -rf:*)"
    ]
  }
}
```

### 배포 규칙 (중요 — 절대 변경 금지)

- **배포는 `git push origin main`으로만 한다.** → 푸시하면 **Cloudflare가 자동 배포**한다.
- Netlify CLI, Vercel CLI, `wrangler deploy` 등으로 직접 배포하지 마.
- 빌드 파이프라인·번들러·프레임워크를 새로 도입하지 마. (`index.html` 하나가 곧 배포물이다.)
- 배포·서버 구성은 기존 방식 그대로 유지하고 임의로 바꾸지 마.

---

## 3. 하네스(Harness) 기본 내용

하네스란 Claude가 실제로 동작하는 실행 환경(도구, 권한, 컨텍스트 관리)을 말한다.

### Claude가 쓰는 주요 도구

- **Read / Write / Edit** — 파일 읽기·생성·수정
- **Bash** — 셸 명령 실행 (권한 모드에 따라 승인 필요)
- **Glob / Grep** — 파일·코드 검색
- **Agent(서브에이전트)** — 큰 탐색·병렬 작업을 별도 에이전트에 위임 → 아래 6번 AI 직원 팀
- **WebSearch / WebFetch** — 웹 검색·페이지 가져오기 (영업·마케팅 조사에 활용)
- **Skill(슬래시 명령)** — `/init`, `/code-review`, `/simplify` 등

### 컨텍스트 로딩 순서

1. `~/.claude/CLAUDE.md` (전역 지침)
2. 프로젝트 루트의 `CLAUDE.md` (이 파일)
3. `docs/` 문서(PROJECT/DATABASE/API/ROADMAP)는 관련 작업 시 참조
4. 메모리 인덱스 (`~/.claude/projects/.../memory/MEMORY.md`)

대화가 길어지면 자동으로 요약(compact)되어 다음 컨텍스트로 이어진다.

### 권한 모드

- 기본: 파일 수정·명령 실행 전 사용자 승인 요청
- `/permissions` 로 허용 목록 관리 (터미널 세션에서)
- 자주 쓰는 안전한 명령은 settings.json의 allow에 등록해 프롬프트 줄이기

### 훅(hooks) — 자동화

특정 이벤트(도구 실행 전/후, 세션 종료 등)에 셸 명령을 자동 실행하려면
settings.json에 hooks를 등록한다. "매번 X 할 때마다 Y 해줘" 같은 자동화는
기억이 아니라 훅으로만 보장된다.

---

## 4. 작업 원칙 (요약)

우선순위: **정확성 > 검증 > 최소 변경 > 명확성 > 유지보수성**

- 파일·API·스키마가 존재한다고 가정하지 말고 먼저 읽어서 확인해. (특히 `index.html`)
- 수정 전에 관련 파일을 읽고, 수정 후에는 브라우저 실행으로 검증해.
- 요청된 작업에만 변경을 국한하고, 관련 없는 리팩토링은 하지 마.
- 가장 단순한 해결책을 선호하고, 불필요한 의존성·추상화·빌드 도구를 추가하지 마.
- 기존 프로젝트의 관례와 스타일을 따라.
- 막히면 멈추고 무엇이 막혔는지, 무엇이 검증됐는지 명확히 보고해.
- 검증 없이 "성공했다"고 주장하지 마.

---

## 5. ERP 개발 원칙 (HM SIGN ERP 코드 규칙)

`index.html` 하나에 모든 것이 들어 있으므로, 파일이 커져도 관리 가능하도록 아래를 지킨다.

- **함수 분리** — 하나의 함수는 하나의 일만. 거대한 함수는 목적 단위로 쪼갠다.
- **주석 작성** — 각 기능 블록 위에 `// ===== [기능명] =====` 형태로 구획 주석을 단다.
- **중복 제거** — 반복되는 로직은 공통 유틸 함수로 뺀다 (예: `fmtWon()`, `supaSelect()`).
- **성능 최적화** — 불필요한 DOM 재렌더·중복 Supabase 호출을 피한다.
- **가독성 유지** — 기존 네이밍·들여쓰기 컨벤션을 따른다.
- **확장 가능 구조** — 새 기능은 기존 코드를 건드리지 않고 *덧붙이는* 방식으로 설계한다.
  향후 추가 예정 기능(아래 7번)이 쉽게 붙도록 훅 포인트를 남긴다.

### 기능 추가 표준 절차 (모든 코드 작업 시)

1. `index.html`에서 관련 기존 코드를 **먼저 읽는다.**
2. 기존 UI/색상/구조를 파악하고, 그 스타일에 맞춰 설계한다.
3. **추가 방식**으로만 구현한다 (기존 함수·마크업 삭제 금지).
4. Supabase 스키마 변경이 필요하면 → 사용자에게 먼저 확인받고 *추가 컬럼/테이블*로만.
5. 로컬에서 브라우저로 열어 기존 기능이 안 깨졌는지 확인한다.
6. `docs/CHANGELOG.md`에 변경 내역을 기록한다.
7. 사용자 승인 후 `git push origin main`.

---

## 6. AI 직원 팀 (Agent Team)

> **최종 목표:** 예쁜 대시보드가 목적이 아니다. **AI 직원들이 실제로 영업·홍보·
> 마케팅해서 매출을 늘리는 것**이 목적이고, ERP 화면은 그 결과를 보여주는 수단일 뿐이다.
> 조직도·ERP 내 "🤖 AI 직원" 대시보드 화면 스펙·구현 방식은
> `docs/AI_COMPANY_OS.md`에 정리되어 있다. 관련 작업 시 반드시 먼저 읽을 것.

이 프로젝트는 7명의 AI 직원(서브에이전트)으로 운영된다. 정의 파일은 `.claude/agents/`에 있다.
큰 작업이 들어오면 Claude(오케스트레이터)는 **적합한 직원에게 위임**하고, 서로 다른 팀 작업은
**병렬로** 실행한다. 각 직원은 자기 역할만 수행하고 다른 직원의 영역을 침범하지 않는다.

**회사 우선순위 (매출 기여도 순 — 갈등 시 이 순서로 자원·시간을 배분한다):**

| 우선순위 | 에이전트 파일 | AI 직원 | 핵심 역할 |
|---|---|---|---|
| 1 | `sales.md` | 영업팀 | 신규 거래처·프랜차이즈·신규 오픈 매장 조사, 영업 대상 추천, 계약 가능성 분석, 재영업 추천, 리스트·메일·제안서 |
| 2 | `marketing.md` | 마케팅팀 | 블로그·인스타·숏폼·유튜브·SEO·네이버 플레이스·광고 문구 (매일 발행 가능한 완성 콘텐츠) |
| 3 | `pr.md` | 홍보팀 | 홍보 전략·SNS 운영 전략·보도자료·이벤트/프로모션 기획·지역 홍보·브랜드 관리 |
| 4 | `secretary.md` | 대표 비서 | 오늘 일정·할 일·견적·미수금·작업/기사 일정 정리 + 전 직원 진행상황 취합 보고 |
| 5 | `analyst.md` | 데이터 분석팀 | ERP 데이터 분석(매출·계약률·거래처·지역·기사 작업량·미수금·재구매·AS) |
| 6 | `erp-developer.md` | ERP 개발팀 | 기능 개발·버그 수정·최적화·UI 개선·API/DB (절대 원칙 준수) |
| 7 | `ui-designer.md` | UI/UX 디자이너 | 화면·사용성·버튼 위치·입력 편의·반응형 개선 (기존 디자인 유지) |

### 위임 규칙

- **큰 작업 = 반드시 에이전트 분리 + 병렬 처리.**
  - 예: "신규 매장 조사 + 블로그 글 + ERP 분석"이 한 번에 들어오면
    → 영업팀 / 마케팅팀 / 분석팀에게 동시에 위임.
- **역할 경계 엄수** — 영업팀은 코드를 만지지 않고, 개발팀은 광고 문구를 쓰지 않는다.
  코드 변경이 필요한 결론이 나오면 개발팀에게 넘긴다. 마케팅(콘텐츠 제작)과 홍보(전략·이벤트·보도자료)도 분리한다.
- **코드에 손대는 건 ERP 개발팀과 UI/UX 디자이너뿐이다.** 이 둘도 위 절대 원칙을 그대로 따른다.
- **기능 우선순위 판단 기준:** "이 기능이 실제로 영업·홍보·마케팅에 어떤 도움이 되는가?"를 먼저 따진다.
  ERP 코드 작업(개발팀·디자이너)은 영업/마케팅/홍보 자동화보다 우선순위가 낮다 — 급한 버그 수정이 아니면
  먼저 영업·마케팅·홍보 실무 결과물을 낼 수 있는지 검토한다.
- 각 에이전트의 상세 지침은 해당 `.md` 파일 본문을 따른다.

---

## 7. 향후 추가 예정 기능 (구조를 미리 열어둘 것)

아래 기능들이 나중에 쉽게 붙도록, 새 코드는 모듈화하고 훅 포인트를 남긴다.

- AI 비서 · AI 자동 영업 · AI 자동 마케팅 · AI 고객관리
- AI OCR · AI 기사 배정 · AI 일정 추천 · AI 주소 추천
- AI 음성 명령 · AI 챗봇 · AI 보고서 자동 생성 · AI 견적 추천
- AI 현장 분석 · AI 일정 최적화 · AI 매출 예측

> 설계 힌트: 이 기능들은 대부분 **Anthropic API 호출 + 기존 Supabase 데이터**로 구현 가능하다.
> `index.html` 안에 `// ===== AI Module =====` 구획을 하나 두고, 기능별 함수를 그 아래 덧붙이는
> 패턴을 권장한다. 자세한 계획은 `docs/ROADMAP.md` 참조.
> "🤖 AI 직원" 대시보드(직원별 현황 카드 + 실시간 작업 로그)는 `docs/AI_COMPANY_OS.md` 참조 —
> 착수 전 Supabase 신규 테이블(`ai_staff_status`, `ai_staff_logs`)에 대해 사용자 승인이 필요하다.

---

## 8. 폴더 구조

```
HM-SIGN-ERP/
│
├── CLAUDE.md                 # 이 파일 (프로젝트 가이드 + 절대 원칙)
├── HANDOFF.md                # 작업 인계 문서 (시스템 구성·최신 상태·주의사항)
│
├── .claude/
│   ├── settings.json         # 권한·훅 (팀 공유)
│   ├── settings.local.json   # 개인 설정 (git 제외)
│   └── agents/               # AI 직원 팀 정의 (Claude Code 서브에이전트, 우선순위순)
│       ├── sales.md
│       ├── marketing.md
│       ├── pr.md
│       ├── secretary.md
│       ├── analyst.md
│       ├── erp-developer.md
│       └── ui-designer.md
│
├── docs/
│   ├── PROJECT.md            # 프로젝트 개요·기능 목록
│   ├── DATABASE.md           # Supabase 스키마
│   ├── API.md                # Supabase·Kakao·Anthropic API 연동 정리
│   ├── CHANGELOG.md          # 변경 이력 (버전별)
│   ├── ROADMAP.md            # 향후 기능 로드맵
│   └── AI_COMPANY_OS.md      # AI Company OS 비전·조직도·대시보드 스펙
│
├── public/
│   └── index.html            # ★ ERP 본체 (단일 파일 — 절대 쪼개지 않음)
│
├── wrangler.jsonc            # Cloudflare Workers 설정 (assets: ./public)
└── package.json
```

> **참고:** Claude Code에서 서브에이전트가 실제로 동작하려면 정의 파일이 `.claude/agents/`에
> 있어야 한다. 세션에서 `/agents` 명령으로 등록된 AI 직원 목록을 확인할 수 있다.

---

## 9. 프로젝트별 정보

- **프로젝트 이름:** HM SIGN ERP
- **기술 스택:** 단일 `public/index.html` (HTML/CSS/JS) + Supabase + Kakao API
- **개발 서버 실행:** 로컬에서 `index.html`을 브라우저로 직접 열거나
  `python3 -m http.server 8000` 후 `http://localhost:8000` 접속
- **빌드:** 없음 (빌드리스 — 단일 파일)
- **테스트:** 브라우저에서 수동 확인 (주요 화면·기능 회귀 체크)
- **배포:** `git push origin main` → Cloudflare 자동 배포
- **주의사항:** 위 **절대 원칙(섹션 0)** 을 항상 최우선으로 지킬 것.

---

## 10. 복원 이력

> 2026-08-10: 회사 PC의 로컬 파일이 삭제되면서 이 문서도 함께 사라졌다.
> Claude 세션 컨텍스트에 남아 있던 원문으로 GitHub에 복원했다.
> 함께 있던 `docs/*.md` 와 `.claude/agents/*.md` 는 컨텍스트에 없어 복원하지 못했다.
> OneDrive 온라인 휴지통(30일)에서 복구 가능하다. 자세한 내용은 `HANDOFF.md` 6절 참조.
>
> **이후 원칙: GitHub private repo 가 유일한 영구 원본이다. 로컬에만 두지 않는다.**
